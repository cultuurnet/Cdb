services:
  php:
    build: docker/php7.4
    container_name: php
    working_dir: /cdb
    volumes:
      - .:/cdb
    stdin_open: true
    tty: true
    command: php -a

volumes:
  cdb:
    driver: local