services:
  php:
    build: docker/php8.1
    container_name: php
    working_dir: /cdb
    volumes:
      - .:/cdb
    stdin_open: true
    tty: true
    command: php -a

volumes:
  cdb:
    driver: local